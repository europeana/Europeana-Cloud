<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:task="http://www.springframework.org/schema/task"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:jee="http://www.springframework.org/schema/jee"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
						http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
						http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd
                        http://www.springframework.org/schema/jee  http://www.springframework.org/schema/jee/spring-jee-3.2.xsd
                        http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-3.2.xsd">

    <context:annotation-config/>

    <context:component-scan
            base-package="data.validator"/>
    <bean id="propertyConfigurer"
          class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="location" value="test.properties"/>
    </bean>


    <bean id="appProperties"
          class="org.springframework.beans.factory.config.PropertiesFactoryBean">
        <property name="singleton" value="true"/>

        <property name="properties">
            <props>
                <prop key="sourceHosts">${sourceHosts}</prop>
                <prop key="sourcePort">${sourcePort}</prop>
                <prop key="sourceKeyspace">${sourceKeyspace}</prop>
                <prop key="sourceUserName">${sourceUserName}</prop>
                <prop key="sourcePassword">${sourcePassword}</prop>
                <prop key="targetHosts">${targetHosts}</prop>
                <prop key="targetPort">${targetPort}</prop>
                <prop key="targetKeyspace">${targetKeyspace}</prop>
                <prop key="targetUserName">${targetUserName}</prop>
                <prop key="targetPassword">${targetPassword}</prop>
            </props>
        </property>
    </bean>

    <bean id="sourceCassandraConnectionProvider" class="eu.europeana.cloud.cassandra.CassandraConnectionProvider"
          scope="prototype">
        <constructor-arg name="hosts" value="${sourceHosts}"/>
        <constructor-arg name="port" value="${sourcePort}"/>
        <constructor-arg name="keyspaceName" value="${sourceKeyspace}"/>
        <constructor-arg name="userName" value="${sourceUserName}"/>
        <constructor-arg name="password" value="${sourcePassword}"/>

    </bean>

    <bean id="targetCassandraConnectionProvider" class="eu.europeana.cloud.cassandra.CassandraConnectionProvider"
          scope="prototype">
        <constructor-arg name="hosts" value="${targetHosts}"/>
        <constructor-arg name="port" value="${targetPort}"/>
        <constructor-arg name="keyspaceName" value="${targetKeyspace}"/>
        <constructor-arg name="userName" value="${targetUserName}"/>
        <constructor-arg name="password" value="${targetPassword}"/>
    </bean>

    <bean id="dataValidator" class="data.validator.DataValidator"
          scope="prototype">
    </bean>


</beans>
